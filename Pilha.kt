class Pilha: Empilhavel {
    //val é uma variavel que nao pode ser mudada
    private val ponteiroTopo: Int
    //se uma variavel nao recebe atributo, ele tera que ser iniciada com sue tipo
    //var é uma variavel cque pode ser alterada no decorrer do codigo
    private var dados: Array<Any>


    contrutor(tamanho; Int){
        PonteiroTopo = -1
        dados = arrayOfNull(tamanho)
    }
}
